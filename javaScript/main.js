const lista = [
    "pomodori",
    "zucchine",
    "pane",
    "uova",
    "pasta",
    "sigarette"
]

for (let i = 0; i < lista.length; i++){
    const elemento = lista[i]
    console.log (elemento)
}